# Apps Script — CHANGELOG (ANDO)

Snapshots congelados del `apps-script.gs` desplegado. `SCRIPT_VERSION` en el
código = versión activa (verificable con `?action=ping`).

| Versión | Fecha | Cambios |
|---------|-------|---------|
| v1 | 2026-07-01 | Base: módulo Medidores + concurrencia (withLock incl. Medidores), upsertSucursal/deleteSucursal, SCRIPT_VERSION en ping |
